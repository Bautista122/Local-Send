// Agrega el prefijo 'export' a la interfaz
export interface MetadataArchivo {
  nombre: string;
  tamanio: number;
  tipo: string;
  uri: string;
}

export const ServicioParaTransferencia = {
  // ... resto del código
};
